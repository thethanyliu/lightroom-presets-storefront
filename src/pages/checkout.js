import React from "react";
import { CartInfo, Footer, Layout } from "@/components";

const Checkout = () => {
  return (
    <>
      <Layout>
        <CartInfo />
      </Layout>
      <Footer />
    </>
  );
};

export default Checkout;
